---
import { Section, SectionContent, SectionProse } from "@/components/ui/section"
---

<Section class="bg-slate-900">
	<SectionContent>
		<SectionProse class="text-white">
			<h3>Blabvlablah</h3>
			<p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Nam dictum convallis turpis tincidunt imperdiet. Praesent faucibus, felis eget pretium pulvinar, ipsum massa fringilla ipsum, at fermentum tellus dui in velit. Cras vehicula, diam in hendrerit vulputate, felis lorem varius nibh, sit amet lobortis libero ligula in odio. Sed scelerisque odio massa, in porttitor lorem varius eu. Vivamus lacinia scelerisque convallis. Ut rhoncus ante id enim commodo sagittis. Nunc convallis vitae dui in aliquet.

Sed interdum ac neque nec aliquet. Duis a neque ligula. Cras rhoncus vulputate odio. Nulla vitae risus at nulla viverra fermentum. Praesent iaculis magna et risus ultrices molestie. Proin gravida eu urna vitae facilisis. Sed blandit metus pulvinar justo lacinia efficitur. Fusce et porttitor mi. Maecenas in auctor tellus. Vestibulum sit amet nulla fringilla, mollis orci vel, tristique tellus. Aenean semper, justo eget sodales dignissim, nisl urna imperdiet nibh, vitae sodales nisl diam sit amet magna. Proin et libero sit amet elit vestibulum scelerisque ut eget ante. Suspendisse nec nibh interdum, hendrerit turpis et, pretium mauris. Vivamus placerat malesuada dolor et mollis. Etiam venenatis lorem at augue ornare, quis vulputate nibh ultricies.</p>
		</SectionProse>
	</SectionContent>
</Section>

---
import { Avatar, AvatarImage } from "@/components/ui/avatar"
import { Button } from "@/components/ui/button"
import { Icon } from "@/components/ui/icon"
import { Image } from "@/components/ui/image"
import {
  Item,
  ItemContent,
  ItemDescription,
  ItemMedia,
  ItemTitle,
} from "@/components/ui/item"
import {
  Section,
  SectionActions,
  SectionContent,
  SectionGrid,
  SectionProse,
} from "@/components/ui/section"
import {
  Tile,
  TileContent,
  TileDescription,
  TileMedia,
  TileTitle,
} from "@/components/ui/tile"

interface Props {
  class?: string
  id?: string
  links?: {
    icon?: string
    text?: string
    href?: string
    target?: string
  }[]
  items?: {
    href?: string
    title?: string
    description?: string
    item?: {
      image?: {
        src: string
        alt: string
      }
      title?: string
      description?: string
    }
    image?: {
      src: string
      alt: string
    }
  }[]
}

const { class: className, id, links, items } = Astro.props
---

<Section class={className} id={id}>
  <SectionContent>
    <SectionProse>
      <slot />
    </SectionProse>
    <SectionActions>
      {
        links?.map(({ icon, text, ...link }, i) => (
          <Button variant={i === 0 ? "default" : "outline"} {...link}>
            {icon && <Icon name={icon} />}
            {text}
          </Button>
        ))
      }
    </SectionActions>
  </SectionContent>
  <SectionGrid>
    {
      items?.map(({ title, description, image, href, item }) => (
        <Tile href={href}>
          <TileMedia class="aspect-video">
            <Image sizes="600px" {...image} />
          </TileMedia>
          <TileContent>
            <TileTitle class="line-clamp-2">{title}</TileTitle>
            <TileDescription class="line-clamp-2">
              {description}
            </TileDescription>
          </TileContent>
          <Item class="p-0">
            <ItemMedia>
              <Avatar class="size-10">
                <AvatarImage src={item?.image?.src} alt={item?.image?.alt} />
              </Avatar>
            </ItemMedia>
            <ItemContent>
              <ItemTitle>{item?.title}</ItemTitle>
              <ItemDescription>{item?.description}</ItemDescription>
            </ItemContent>
          </Item>
        </Tile>
      ))
    }
  </SectionGrid>
</Section>
